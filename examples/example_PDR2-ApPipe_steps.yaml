- SpecBlock:
      name: PDR2-ApPipe_step1
      includes: ['step']
      data:
          pipeline_yaml: "${AP_PIPE_DIR}/pipelines/HSC/ApPipe.yaml#isr,calibrateImage,\
            retrieveTemplate,subtractImages,detectAndMeasure,filterDiaSrcCat,\
            rbClassify,transformDiaSrcCat,analyzeAssocDiaSrcCore,analyzeTrailedDiaSrcCore,\
            diffimTaskCore,initialPviCore"
      child_config:
          base_query: "instrument='HSC' and skymap = 'hsc_rings_v1'"
          split_dataset: raw
          split_field: exposure
