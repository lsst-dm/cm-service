- SpecBlock:
      name: comcamsim_step1
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step1"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step2a
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step2a"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step2b
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step2b"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step2c
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step2c"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step2d
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step2d"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step2e
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step2e"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step3
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step3"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step4
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step4"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step5
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step5"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step6
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step6"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
- SpecBlock:
      name: comcamsim_step7
      includes: ['step']
      data:
          pipeline_yaml: "${DRP_PIPE_DIR}/pipelines/LSSTComCamSim/DRP-ops-rehearsal-3.yaml#step7"
      child_config:
          base_query: "instrument='LSSTComCamSim' and skymap='DC2_cells_v1'"
          split_method: no_split
