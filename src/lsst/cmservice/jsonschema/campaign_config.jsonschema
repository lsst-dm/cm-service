{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/lsst-dm/cm-service",
  "title": "Campaign Configuration",
  "description": "Configuration for a campaign's Spec field.",
  "type": "object",
  "properties": {
    "auto_transition": {
      "type": "boolean",
      "default": true,
      "description": "Whether to automatically transition Nodes using the CM daemon. If false, the campaign may remain in a 'paused' state."
    }
  }
}
